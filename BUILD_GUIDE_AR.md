<div dir="rtl" align="right">

# 🏗️ دليل بناء ملف EXE على نظام Windows

## خطوة بخطوة — من الصفر حتى ملف التثبيت

---

## 📋 المحتويات

1. [تجهيز البيئة](#-الخطوة-١--تجهيز-البيئة)
2. [تحميل المشروع من GitHub](#-الخطوة-٢--تحميل-المشروع-من-github)
3. [تثبيت المكتبات](#-الخطوة-٣--تثبيت-مكتبات-المشروع)
4. [تجربة التشغيل المحلي](#-الخطوة-٤--تجربة-التشغيل-المحلي)
5. [بناء ملف EXE](#-الخطوة-٥--بناء-ملف-exe)
6. [تسليم الملف النهائي](#-الخطوة-٦--تسليم-الملف-النهائي)
7. [حل المشاكل الشائعة](#-حل-المشاكل-الشائعة)

---

## 🔧 الخطوة ١ — تجهيز البيئة

### ١.١ تثبيت Node.js

1. افتح المتصفح واذهب إلى: **[https://nodejs.org](https://nodejs.org)**
2. حمّل النسخة **LTS** (الموصى بها — الزر الأخضر الكبير)
3. شغّل ملف التثبيت واختر الخيارات الافتراضية (Next → Next → Install)
4. ✅ تأكد من تفعيل خيار **"Add to PATH"** أثناء التثبيت

**للتأكد من نجاح التثبيت:**
```cmd
node --version
npm --version
```
يجب أن يظهر رقم الإصدار (مثلاً `v20.11.0` و `10.2.0`)

---

### ١.٢ تثبيت Python

1. افتح المتصفح واذهب إلى: **[https://www.python.org/downloads](https://www.python.org/downloads)**
2. حمّل أحدث إصدار Python 3
3. ⚠️ **مهم جدًا:** عند بداية التثبيت، ضع علامة ✅ على **"Add Python to PATH"** في أسفل نافذة التثبيت
4. اضغط **Install Now**

**للتأكد من نجاح التثبيت:**
```cmd
python --version
pip --version
```

**تثبيت مكتبة requests:**
```cmd
pip install requests
```

---

### ١.٣ تثبيت Git

1. افتح المتصفح واذهب إلى: **[https://git-scm.com/download/win](https://git-scm.com/download/win)**
2. حمّل الملف وشغّله
3. اختر الخيارات الافتراضية في كل الخطوات (Next → Next → Install)

**للتأكد من نجاح التثبيت:**
```cmd
git --version
```

---

## 📥 الخطوة ٢ — تحميل المشروع من GitHub

### ٢.١ فتح سطر الأوامر

اضغط على زر **Windows + R** واكتب `cmd` ثم اضغط Enter

### ٢.٢ اختيار مكان التحميل

```cmd
cd Desktop
```

### ٢.٣ استنساخ المشروع

```cmd
git clone https://github.com/YOUR_USERNAME/mersal-info-center.git
```

> ⚠️ استبدل `YOUR_USERNAME` باسم المستخدم الفعلي على GitHub

### ٢.٤ الدخول لمجلد المشروع

```cmd
cd mersal-info-center
```

---

## 📦 الخطوة ٣ — تثبيت مكتبات المشروع

```cmd
npm install
```

> ⏳ انتظر حتى تنتهي العملية — قد تأخذ ٢-٥ دقائق حسب سرعة الإنترنت
>
> ✅ ستلاحظ ظهور مجلد `node_modules` داخل المشروع

---

## ▶️ الخطوة ٤ — تجربة التشغيل المحلي

```cmd
npm start
```

> إذا فتح التطبيق بنجاح وعرض الواجهة — فكل شيء يعمل! 🎉
> 
> أغلق التطبيق بالضغط على زر الإغلاق (×) أو اضغط `Ctrl + C` في سطر الأوامر

---

## 🏗️ الخطوة ٥ — بناء ملف EXE

### ٥.١ أمر البناء

```cmd
npm run build:win
```

> ⏳ هذه العملية تأخذ وقتًا (٣-١٠ دقائق) حسب سرعة الجهاز
>
> سيقوم electron-builder بتحميل الملفات اللازمة تلقائيًا في أول مرة

### ٥.٢ ماذا ينتج عن البناء؟

بعد انتهاء البناء بنجاح، ستجد الملفات في مجلد `build/`:

```
build/
├── Mersal Info Center Setup X.X.X.exe    ← ملف التثبيت (NSIS Installer)
├── Mersal Info Center X.X.X.exe          ← نسخة محمولة (Portable)
└── ...                                    ← ملفات أخرى مساعدة
```

| الملف | الوصف | الاستخدام |
|-------|-------|----------|
| **Setup X.X.X.exe** | ملف تثبيت كامل | يثبت التطبيق في Program Files مع اختصار على سطح المكتب |
| **X.X.X.exe** (Portable) | نسخة محمولة | تعمل مباشرة بدون تثبيت — مناسبة للفلاشة |

---

## 📤 الخطوة ٦ — تسليم الملف النهائي

### خيار أ: مشاركة ملف التثبيت

1. افتح مجلد `build/` داخل مجلد المشروع
2. انسخ ملف **`Mersal Info Center Setup X.X.X.exe`**
3. شاركه عبر Google Drive أو OneDrive أو أي وسيلة أخرى

### خيار ب: مشاركة النسخة المحمولة

1. انسخ ملف **`Mersal Info Center X.X.X.exe`** (Portable)
2. يمكن وضعه على فلاشة وتشغيله مباشرة على أي جهاز Windows

---

## 🔄 تحديث التطبيق لاحقًا

عند وجود تحديثات على GitHub:

```cmd
cd Desktop\mersal-info-center

# سحب آخر التحديثات
git pull

# تحديث المكتبات (إن لزم)
npm install

# إعادة البناء
npm run build:win
```

---

## 🔧 حل المشاكل الشائعة

### ❌ مشكلة: `node` غير معروف كأمر

**السبب:** Node.js غير مضاف إلى PATH

**الحل:**
1. أعد تثبيت Node.js
2. تأكد من تفعيل خيار **"Add to PATH"**
3. أغلق سطر الأوامر وافتحه مجددًا

---

### ❌ مشكلة: `npm install` يفشل

**الحل:**
```cmd
# مسح الكاش
npm cache clean --force

# حذف node_modules وإعادة التثبيت
rmdir /S /Q node_modules
del package-lock.json
npm install
```

---

### ❌ مشكلة: `npm run build:win` يفشل

**الأسباب المحتملة:**
1. **لم يتم تثبيت المكتبات:** شغّل `npm install` أولاً
2. **مشكلة في الأيقونة:** تأكد من وجود `src/assets/logo.webp`
3. **صلاحيات:** شغّل سطر الأوامر كمسؤول (Run as Administrator)

**الحل الكامل:**
```cmd
# تشغيل كمسؤول
# اضغط Start → اكتب cmd → Right Click → Run as Administrator

cd Desktop\mersal-info-center
npm install
npm run build:win
```

---

### ❌ مشكلة: التطبيق لا يحمّل البيانات

**السبب:** Python غير مثبت أو مكتبة `requests` غير موجودة

**الحل:**
```cmd
python --version
pip install requests
```

---

### ❌ مشكلة: Windows Defender يمنع التشغيل

عند تشغيل ملف EXE قد يظهر تحذير **SmartScreen**:

1. اضغط على **"More info"** (معلومات إضافية)
2. اضغط على **"Run anyway"** (تشغيل على أي حال)

> هذا طبيعي للتطبيقات غير الموقّعة رقميًا (unsigned)

---

## 📞 الدعم الفني

في حالة وجود أي مشكلة:
- افتح **Issue** جديد على صفحة GitHub الخاصة بالمشروع
- أو تواصل مع فريق التطوير مباشرة

---

> 📅 آخر تحديث: مارس ٢٠٢٦

</div>
